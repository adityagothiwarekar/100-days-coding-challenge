#include <stdio.h>
int t,x;

int main(void) {
	scanf("%d",&t);
	{
	    while(t!=0)
	    {
	        scanf("%d %d",&a,&b);
	        if(a>b)
	        {
	            printf("%d",b);
	        }
	        else if(b>a)
	        {
	            printf("%d",a);
	        }
	        else
	        {
	            printf("%d",a);
	        }
	        
	    }
	}
	return 0;
}
